<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="resources/vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="resources/vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache">
    <php>
        <!-- Pins App's config resolution to Configuration/config.env.testing for
             the whole run: App is a process-lifetime singleton with no reset
             hook, so this must be set before any test touches it, not inside
             a single test's setUp(). -->
        <env name="APP_ENV" value="testing" force="true"/>
    </php>
    <testsuites>
        <testsuite name="Unit">
            <directory>Tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>Tests/Feature</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>Tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory suffix=".php">Core</directory>
        </include>
    </source>
</phpunit>
